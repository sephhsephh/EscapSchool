using Unity.Netcode;
using UnityEngine;

public class PlayerNetworkController : NetworkBehaviour
{
    // No need for a method, just use the built-in property
    // (Remove the IsLocalPlayer() method entirely)
}